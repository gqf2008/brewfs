target/
**/target/
.worktrees/
juicefs/

# Local runtime data and logs
data/
slayerfs-meta/
*.env
results/
*.log
*.pid
*.sock

# Large/generated test artifacts
docker/compose-xfstests/artifacts/
docker/compose-pjdfstest/artifacts/
tests/scripts/xfstests-prebuilt/*.tar.gz
!tests/scripts/xfstests-prebuilt/xfstests-prebuilt.tar.gz
perf.data

# Web console generated assets
web/console/node_modules/
web/console/dist/

# Editor and OS noise
.DS_Store
*.swp

# Local-only credentials and config (never commit)
.local/

# macOS release artifacts (regenerable via scripts/package_macos.sh)
dist/

# Windows installer build output (generated by desktop/installer/build-installer.ps1)
desktop/installer/build/

# AppleDouble metadata (ExFAT/macOS artifact), never commit
._*

# Redis dump dropped in repo root by local test instances
*.rdb
